import {Meta, Unstyled} from '@storybook/addon-docs';

<Meta title="Documentation/Migrations/v6 to v7"/>

# Migrate from v6 to v7

WCS v7 contains breaking changes only in the following packages:
- wcs-core
- wcs-angular
- wcs-react

Version 7 introduces a comprehensive redesign of the library, incorporating a new concept: design tokens. To learn more about design tokens and their benefits, we recommend consulting [this documentation](.?path=/docs/documentation-theming--documentation#how-it-works).

To migrate from the current system to the new one, simply follow the steps :
- for **Angular** users, follow the [Angular guide](.?path=/docs/documentation-getting-started-angular--documentation#6-add-a-theme)
- for **React** users, follow the [React guide](.?path=/docs/documentation-getting-started-react--documentation#5-add-a-theme)


**Important Note for CSS Variable Overrides**

If your project uses CSS variable overrides, it is crucial to read this documentation thoroughly. Significant breaking changes have been introduced in this area, and understanding these changes is essential for a smooth transition.

## wcs-core

### Fonts

Some changes have been made to the font weights. You must update your global stylesheet accordingly.

1. **`Avenir Heavy`** has been added to the available fonts. It corresponds to the **font-weight `800`** of the Avenir font family.  
You must add it to your font-face declaration if you want to use it.

```css
@font-face {
  font-family: Avenir;
  src: url("assets/fonts/avenir-heavy.woff");
  font-weight: 800;
}
```

2. **`Avenir Roman`** has been added to the available fonts. It corresponds to the **font-weight `400`** of the Avenir font family.  
You must add it to your font-face declaration if you want to use it.

```css
@font-face {
  font-family: Avenir;
  src: url("assets/fonts/avenir-roman.woff");
  font-weight: 400;
}
```

3. **`Avenir Light`** is now **font-weight `100`** instead of `300`. **`Avenir Book`** is now **font-weight `300`** instead of `400`.

To sum up :


<table>
    <thead>
        <tr>
            <th>Before</th>
            <th>After</th>
        </tr>
    </thead>
    <tbody style={{verticalAlign: 'baseline', fontFamily: 'Avenir'}}>
        
        <tr>
            <td>
                <Unstyled>
                    <span style={{fontWeight: 900}}>Avenir Black 900</span><br/>
                    <span style={{fontWeight: 500}}>Avenir Medium 500</span><br/>
                    <span style={{fontWeight: 300}}>Avenir Book 400</span><br/>
                    <span style={{fontWeight: 100}}>Avenir Light 300</span><br/>
                </Unstyled>
            </td>
            <td>
                <Unstyled>
                    <span style={{fontWeight: 900}}>Avenir Black 900</span><br/>
                    <span style={{fontWeight: 800}}>Avenir Heavy 800</span><br/>
                    <span style={{fontWeight: 500}}>Avenir Medium 500</span><br/>
                    <span style={{fontWeight: 400}}>Avenir Roman 400</span><br/>
                    <span style={{fontWeight: 300}}>Avenir Book 300</span><br/>
                    <span style={{fontWeight: 100}}>Avenir Light 100</span><br/>
                </Unstyled>
            </td>
        </tr>
    </tbody>
</table>

Please read the [Framework integration documentation](.?path=/docs/documentation-integrations-framework-integrations--documentation) for further information.

### CSS Variables

> During Design Tokens migration, we have **reviewed and updated certain CSS variables names on several components**.  
> - If you have used **any of the following CSS variables below**, you need to **update them** in your project or use the new ones.  
> - If these variable doesn't appear in your codebase, you don't need to update anything.

#### Globals

Colors:
- `--wcs-white` has been **deleted**. Use the correct design token according to your needs, for example:
    - `--wcs-semantic-color-text-inverse` for white text color over a dark background (note that the token no longer contains the color value, only the usage context. Can be usefull for theming).
    - `--wcs-semantic-color-background-surface-primary` for white background color.
    - `--wcs-semantic-color-foreground-action-primary` for white foreground color on actions.
- `--wcs-contrast` has been **deleted**. Use `--wcs-semantic-color-text-inverse` instead.
- `--wcs-light` has been **deleted**. Use the correct design token according to your needs, for example:
    - `--wcs-semantic-color-background-surface-secondary` for light background color.
- `--wcs-gray-light` has been **deleted**. Use the correct design token according to your needs, for example:
  - `--wcs-semantic-color-text-secondary` for light text color.
- `--wcs-gray` has been **deleted**. Use the correct design token according to your needs, for example:
  - `--wcs-semantic-color-text-primary` for text color.
- `--wcs-gray-dark` has been **deleted**. Use the correct design token according to your needs.
- `--wcs-gray-darker` has been **deleted**. Use the correct design token according to your needs.
- `--wcs-black` has been **deleted**. Use the correct design token according to your needs.
- `--wcs-text-light` has been **deleted**. Use the correct design token according to your needs, for example:
  - `--wcs-semantic-color-border-surface` for surface border color.
- `--wcs-text-disabled` has been **deleted**. Use the correct design token according to your needs, for example:
  - `--wcs-semantic-color-background-surface-tertiary` for background of prefix and suffix of control.
- `--wcs-text-medium` has been **deleted**. Use the correct design token according to your needs, for example:
  - `--wcs-semantic-color-text-teriary` for tertiary (light gray) texts.
- `--wcs-blue` has been **deleted**. Use the correct design token according to your needs, for example:
    - `--wcs-semantic-color-primary` for primary actions or elements in the primary color.
- `--wcs-purple` has been **deleted**. Use the correct design token according to your needs.
- `--wcs-pink` has been **deleted**. Use the correct design token according to your needs.
- `--wcs-red` has been **deleted**. Use the correct design token according to your needs, for example:
    - `--wcs-semantic-color-text-critical` for representing a red text with error or critical actions.
- `--wcs-orange` has been **deleted**. Use the correct design token according to your needs.
- `--wcs-yellow` has been **deleted**. Use the correct design token according to your needs, for example:
        - `--wcs-semantic-color-background-warning-default` for representing warnings.
- `--wcs-green` has been **deleted**. Use the correct design token according to your needs, for example:
    - `--wcs-semantic-color-background-success-default` for representing success states.
- `--wcs-teal` has been **deleted**. Use the correct design token according to your needs.
- `--wcs-cyan` has been **deleted**. Use the correct design token according to your needs.
- `--wcs-primary` has been **deleted**. Use `--wcs-semantic-color-primary` instead.
- `--wcs-base` has been **deleted**. Use `--wcs-semantic-color-primary` instead.

Font weight:
- `--wcs-font-weight-form-value` has been **deleted**. Use the correct font weight token according to your needs, for example:
    - `--wcs-semantic-font-weight-medium` for medium font weight.

Size:
- `--wcs-base-size` has been **deleted**. Use `wcs-semantic-size-base` instead.
- `--wcs-size-xl` has been **deleted**. Use `--wcs-semantic-size-xl` instead.
- `--wcs-size-l` has been **deleted**. Use `--wcs-semantic-size-l` instead.
- `--wcs-size-m` has been **deleted**. Use `--wcs-semantic-size-m` instead.
- `--wcs-size-s` has been **deleted**. Use `--wcs-semantic-size-s` instead.
- `--wcs-size-xs` has been **deleted**. Use `--wcs-semantic-size-xs` instead.

Border:
- `--wcs-border-size` has been **deleted**. Use `--wcs-semantic-border-width-large` instead.
- `--wcs-border-radius` has been **deleted**. Use `--wcs-semantic-border-radius-base` instead.

Padding:
- `--wcs-padding` has been **deleted**. Use `wcs-semantic-spacing-base` instead.
- `--wcs-padding-l` has been **deleted**. Use a calc function on the base spacing.
- `--wcs-padding-m` has been **deleted**. Use `wcs-semantic-spacing-base` instead.
- `--wcs-padding-s` has been **deleted**. Use `--wcs-semantic-spacing-small` instead.
- `--wcs-text-padding` has been **deleted**. Use the correct padding design token according to your needs.

Margin:
- `--wcs-base-margin` has been **deleted**. Use `wcs-semantic-spacing-base` instead.
- `--wcs-margin` has been **deleted**. Use `--wcs-semantic-spacing-large` instead.
- `--wcs-margin-l` has been **deleted**. Use a calc function on the base spacing.
- `--wcs-margin-m` has been **deleted**. Use `wcs-semantic-spacing-base` instead.
- `--wcs-margin-s` has been **deleted**. Use `--wcs-semantic-spacing-small` instead


#### wcs-button

- `--wcs-button-height` has been **deleted**. Use these one instead
    - use instead `--wcs-button-height-s`
    - use instead `--wcs-button-height-m`
    - use instead `--wcs-button-height-l`

#### wcs-card

- Default card mode is now `flat` instead of `raised`. Update your cards if needed.

#### wcs-input

- `--wcs-input-icon-color` has been **deleted**. Use these one instead
    - `--wcs-input-icon-color-default`: default icon color
    - `--wcs-input-icon-color-focus`: icon color on focus
    - `--wcs-input-icon-color-disabled`: icon color when disabled

#### wcs-textarea

- `--wcs-textarea-icon-color` has been **deleted**. Use these one instead
    - `--wcs-textarea-icon-color-default`: default icon color
    - `--wcs-textarea-icon-color-focus`: icon color on focus
    - `--wcs-textarea-icon-color-disabled`: icon color when disabled

#### wcs-select

- `--wcs-select-icon-color` has been **deleted**. Use these one instead
    - `--wcs-select-icon-color-default`: default icon color
    - `--wcs-select-icon-color-focus`: icon color on focus
    - `--wcs-select-icon-color-disabled`: icon color when disabled
- `--wcs-select-border-color` has been **deleted**. Use these one instead
    - `--wcs-select-control-border-color-default`: default border color
    - `--wcs-select-control-border-color-focus`: border color on focus
    - `--wcs-select-control-border-color-disabled`: border color when disabled
- `--wcs-select-outline-color` has been **deleted**. You have to use `--wcs-select-control-border-color-focus` instead.
- `--wcs-select-option-height` has been **deleted** from `wcs-select`. You have to use `--wcs-select-option-height` from `wcs-select-option` instead.
- `--wcs-select-border-width` has been **deleted** from `wcs-select`. Use these one instead
    - `--wcs-select-overlay-border-width`: border width of the overlay which contains the options
    - `--wcs-select-control-border-width`: default border width
    - `--wcs-select-control-border-width-active`: border width when select is opened
    - `--wcs-select-control-border-width-focus`: border width when select is focused
- `--wcs-select-border-radius` has been **deleted**. Use these one instead
    - `--wcs-select-control-border-radius`: default border radius
    - `--wcs-select-overlay-border-radius`: border radius of the overlay which contains the options

#### wcs-native-select

- `--wcs-select-native-outline-color` has been **deleted**. Use these one instead
    - `--wcs-select-native-border-color-default`: default border color
    - `--wcs-select-native-border-color-focus`: border color on focus
    - `--wcs-select-native-border-color-disabled`: border color when disabled

#### wcs-switch

- `--wcs-switch-bullet-color-initial` and `--wcs-switch-bullet-color-final` has been deleted. Use instead : 
    - `--wcs-switch-dot-color-initial`: for dot color inside switch when switch is not selected
    - `--wcs-switch-dot-color-final`: for dot color inside switch when switch is selected
- `--wcs-switch-text-color` has been deleted. Use instead : 
    - `--wcs-switch-text-color-default`: text color default
    - `--wcs-switch-text-color-selected`: text color when switch is selected
    - `--wcs-switch-text-color-hover`: text color when switch is hovered
    - `--wcs-switch-text-color-disabled`: text color when switch is disabled

#### wcs-badge

- Rename `--wcs-badge-ligther-percentage` to `--wcs-badge-lighter-percentage` (typo)

#### wcs-header

With design tokens migration, the `wcs-header` **is no longer dark**. As you can put `wcs-button` inside the `wcs-header`, you have to verify the button is still visible. 

Indeed, on storybook, we applied the `.wcs-light` class to the button to make it visible. As the `wcs-header` is no longer dark, you have to remove this class from the button to keep it visible if you have used it.

Before

```html
<wcs-header>
    <img
        slot="logo"
        alt="SNCF"
        src="data:image/svg+xml;base64,PD94...."
    />
    <h1 slot="title">Votre superbe application</h1>
    <div slot="actions">
        <wcs-button mode="clear" class="wcs-light"> <!--The class wcs-light was here-->
            <span>Connexion</span>
            <wcs-mat-icon icon="person_outline"></wcs-mat-icon>
        </wcs-button>
    </div>
</wcs-header>
```

After

```html
<wcs-header>
    <img
        slot="logo"
        alt="SNCF"
        src="data:image/svg+xml;base64,PD94...."
    />
    <h1 slot="title">Votre superbe application</h1>
    <div slot="actions">
        <wcs-button mode="clear"> <!--The class is no longer applied-->
            <span>Connexion</span>
            <wcs-mat-icon icon="person_outline"></wcs-mat-icon>
        </wcs-button>
    </div>
</wcs-header>
```

### wcs-tabs

- `--wcs-tabs-width` has been **deleted**. This variable was not used because `width` is always set to `auto` for the tabs.
- `--wcs-tabs-padding-horizontal` has been **deleted**. Use `--wcs-tabs-padding-left`/`--wcs-tabs-padding-right` instead.
- `--wcs-tabs-padding-vertical` has been **deleted**. Use `--wcs-tabs-padding-top`/`--wcs-tabs-padding-bottom` instead.
